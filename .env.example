# Copy to .env (do NOT commit .env)
OSF_PROJECT=your_osf_project_id   # e.g., abcd3
# Optional: leave blank if project root holds files
OSF_COMPONENT=                     # e.g., xyz12 (OSF component id)
# Personal Access Token from OSF settings
OSF_TOKEN=                         # e.g., osf-xxxxx (keep secret)
